#include <stdio.h>
int main()
{
    int i, n, sum = 0, sum1 = 0;
    int a = 0, b = 1;
    scanf("%d", &n);

    for (i = 3; i <= n; i++)
    {

        // printf("%d ", sum);
        a = b;
        b = sum;

        sum = a + b;
        printf("%d ", sum);

        if (sum <= n)
        {
            printf("%d ", a
    }
}
    }
} + b);
        }
    }
}