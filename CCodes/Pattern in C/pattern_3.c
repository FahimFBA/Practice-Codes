
// Pattern 5
#include<stdio.h>
int main()
{
    int n, row, col;
    scanf("%d", &n);
    for(row=0; row<n; row++)
    {
        for(col=0; col<row; col++)
        {
            if (col % 2 != 0)
            {
                printf("%c" , );
            }

            else
            {
                printf("B");
            }
            
        }
    }
}